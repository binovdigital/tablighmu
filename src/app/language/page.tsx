import Language from "@/components/language";
import React from "react";

export const metadata = {
	title: "Affan Language - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Language />
		</>
	);
};

export default index;
