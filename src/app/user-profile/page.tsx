import React from "react";
import UserProfile from "@/components/user-profile";

export const metadata = {
	title: "Affan User Profile - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<UserProfile />
		</>
	);
};

export default index;
