import Fallback from "@/components/fallback";
import React from "react";

export const metadata = {
	title: "Affan Fallback - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Fallback />
		</>
	);
};

export default index;
