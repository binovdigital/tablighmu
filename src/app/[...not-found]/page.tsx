import Error from "@/components/error";

export const metadata = {
	title: "404 error || Affan - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Error />
		</>
	);
};

export default index;
