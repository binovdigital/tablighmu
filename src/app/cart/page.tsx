import Cart from "@/components/cart";
import React from "react";

export const metadata = {
	title: "Affan Cart - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Cart />
		</>
	);
};

export default index;
