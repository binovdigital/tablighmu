import VideoCall from "@/components/video-call";
import React from "react";

export const metadata = {
	title: "Affan Video Call - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<VideoCall />
		</>
	);
};

export default index;
