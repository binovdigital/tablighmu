import Chat from "@/components/chat";
import React from "react";

export const metadata = {
	title: "Affan Chat - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Chat />
		</>
	);
};

export default index;
