import SocialLogin from "@/components/social-login";
import React from "react";

export const metadata = {
	title: "Affan Social Login - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<SocialLogin />
		</>
	);
};

export default index;
