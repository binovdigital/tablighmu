import React from "react";
import Invoice from "@/components/invoice";

export const metadata = {
	title: "Affan Invoice - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Invoice />
		</>
	);
};

export default index;
