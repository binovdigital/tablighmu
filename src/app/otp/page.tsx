import Otp from "@/components/otp";
import React from "react";

export const metadata = {
	title: "Affan Otp - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Otp />
		</>
	);
};

export default index;
