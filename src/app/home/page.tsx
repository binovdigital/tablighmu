import Home from "@/components/home";
import React from "react";

export const metadata = {
	title: "Affan Home - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Home />
		</>
	);
};

export default index;
