import React from "react";
import BlogGrid from "@/components/blog-grid";

export const metadata = {
	title: "Affan Blog Grid - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<BlogGrid />
		</>
	);
};

export default index;
