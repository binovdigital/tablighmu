import React from "react";
import Checkout from "@/components/checkout";

export const metadata = {
	title: "Affan Checkout - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Checkout />
		</>
	);
};

export default index;
