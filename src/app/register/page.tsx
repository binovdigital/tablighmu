import Register from "@/components/register";
import React from "react";

export const metadata = {
	title: "Affan Register - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Register />
		</>
	);
};

export default index;
