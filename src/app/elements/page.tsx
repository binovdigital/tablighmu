import React from "react";
import Elements from "@/components/elements";

export const metadata = {
	title: "Affan Elements - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Elements />
		</>
	);
};

export default index;
