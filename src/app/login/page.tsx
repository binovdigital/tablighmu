import Login from "@/components/login";
import React from "react";

export const metadata = {
	title: "Affan Login - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Login />
		</>
	);
};

export default index;
