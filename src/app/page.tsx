import React from "react";
import Home from "@/components/home";
export const metadata = {
	title: "Tablighmu App",
};

const index = () => {
	return (
		<>
			<Home />
		</>
	);
};

export default index;
