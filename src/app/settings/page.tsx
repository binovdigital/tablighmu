import Settings from "@/components/settings";
import React from "react";

export const metadata = {
	title: "Affan Settings - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Settings />
		</>
	);
};

export default index;
