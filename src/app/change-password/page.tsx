import React from "react";
import ChangePassword from "@/components/change-password";

export const metadata = {
	title: "Affan Change Password - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<ChangePassword />
		</>
	);
};

export default index;
