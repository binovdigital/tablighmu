import ShopGrid from "@/components/shop-grid";
import React from "react";

export const metadata = {
	title: "Affan Shop Grid - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<ShopGrid />
		</>
	);
};

export default index;
