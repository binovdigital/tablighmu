import React from "react";
import Notifications from "@/components/notifications";

export const metadata = {
	title: "Affan Notifications - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Notifications />
		</>
	);
};

export default index;
