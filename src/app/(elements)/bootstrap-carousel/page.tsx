import BootstrapCarousel from "@/components/bootstrap-carousel";
import React from "react";

export const metadata = {
	title: "Affan Bootstrap Carousel - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<BootstrapCarousel />
		</>
	);
};

export default index;
