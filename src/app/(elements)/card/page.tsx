import Card from "@/components/card";
import React from "react";

export const metadata = {
	title: "Affan Card - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Card />
		</>
	);
};

export default index;
