import FormRadio from "@/components/form-radio";
import React from "react";

export const metadata = {
	title: "Affan Form Radio - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<FormRadio />
		</>
	);
};

export default index;
