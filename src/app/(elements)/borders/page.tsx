import React from "react";
import Borders from "@/components/borders";

export const metadata = {
	title: "Affan Borders - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Borders />
		</>
	);
};

export default index;
