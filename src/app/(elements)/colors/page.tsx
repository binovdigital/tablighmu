import Colors from "@/components/colors";
import React from "react";

export const metadata = {
	title: "Affan Colors - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Colors />
		</>
	);
};

export default index;
