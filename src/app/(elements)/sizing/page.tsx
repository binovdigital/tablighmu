import Sizing from "@/components/sizing";
import React from "react";

export const metadata = {
	title: "Affan Sizing - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Sizing />
		</>
	);
};

export default index;
