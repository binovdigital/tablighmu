import Countdown from "@/components/countdown";
import React from "react";

export const metadata = {
	title: "Affan Count Down - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Countdown />
		</>
	);
};

export default index;
