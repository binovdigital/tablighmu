import Accordion from "@/components/accordion";
import React from "react";

export const metadata = {
	title: "Affan Accordion - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Accordion />
		</>
	);
};

export default index;
