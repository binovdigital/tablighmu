import React from "react";
import FormInputGroup from "@/components/form-input-group";

export const metadata = {
	title: "Affan Form Input Group - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<FormInputGroup />
		</>
	);
};

export default index;
