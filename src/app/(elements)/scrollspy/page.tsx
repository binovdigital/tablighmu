import Scrollspy from "@/components/scrollspy";
import React from "react";

export const metadata = {
	title: "Affan Scrollspy - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Scrollspy />
		</>
	);
};

export default index;
