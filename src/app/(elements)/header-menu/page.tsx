import React from "react";
import HeaderMenu from "@/components/header-menu";

export const metadata = {
	title: "Affan Header Menu - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<HeaderMenu />
		</>
	);
};

export default index;
