import React from "react";
import FormInput from "@/components/form-input";

export const metadata = {
	title: "Affan Form Input - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<FormInput />
		</>
	);
};

export default index;
