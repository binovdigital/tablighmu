import React from "react";
import Breadcrumb from "@/components/breadcrumb";

export const metadata = {
	title: "Affan Breadcrumb - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Breadcrumb />
		</>
	);
};

export default index;
