import ProgressBar from "@/components/progress-bar";
import React from "react";

export const metadata = {
	title: "Affan Progress Bar - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<ProgressBar />
		</>
	);
};

export default index;
