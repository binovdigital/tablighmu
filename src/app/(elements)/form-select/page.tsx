import React from "react";
import FormSelect from "@/components/form-select";

export const metadata = {
	title: "Affan Form Select - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<FormSelect />
		</>
	);
};

export default index;
