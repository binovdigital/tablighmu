import React from "react";
import FooterMenu from "@/components/footer-menu";

export const metadata = {
	title: "Affan Footer Menu - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<FooterMenu />
		</>
	);
};

export default index;
