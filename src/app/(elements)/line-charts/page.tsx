import LineCharts from "@/components/line-charts";
import React from "react";

export const metadata = {
	title: "Affan Line Charts - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<LineCharts />
		</>
	);
};

export default index;
