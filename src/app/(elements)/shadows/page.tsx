import Shadows from "@/components/shadows";
import React from "react";

export const metadata = {
	title: "Affan Shadows - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Shadows />
		</>
	);
};

export default index;
