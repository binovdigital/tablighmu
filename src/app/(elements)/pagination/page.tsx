import Pagination from "@/components/pagination";
import React from "react";

export const metadata = {
	title: "Affan Pagination - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Pagination />
		</>
	);
};

export default index;
