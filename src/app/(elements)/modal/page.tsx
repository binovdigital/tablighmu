import React from "react";
import Modal from "@/components/modal";

export const metadata = {
	title: "Affan Modal - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Modal />
		</>
	);
};

export default index;
