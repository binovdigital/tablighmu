import FormRange from "@/components/form-range";
import React from "react";

export const metadata = {
	title: "Affan Form Range - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<FormRange />
		</>
	);
};

export default index;
