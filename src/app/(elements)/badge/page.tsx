import Badge from "@/components/badge";
import React from "react";

export const metadata = {
	title: "Affan Badge - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Badge />
		</>
	);
};

export default index;
