import React from "react";
import SearchResult from "@/components/search-result";

export const metadata = {
	title: "Affan Search Result - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<SearchResult />
		</>
	);
};

export default index;
