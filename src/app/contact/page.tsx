import React from "react";
import Contact from "@/components/contact";

export const metadata = {
	title: "Affan Contact - PWA Mobile Next js Template",
};

const index = () => {
	return (
		<>
			<Contact />
		</>
	);
};

export default index;
